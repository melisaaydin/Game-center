{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "scripts": {
    "start": "lerna run start --scope=TOYOTA",
    "test": "lerna run test --scope=TOYOTA",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  },
  "dependencies": {
    "pg": "^8.13.3"
  }
}
